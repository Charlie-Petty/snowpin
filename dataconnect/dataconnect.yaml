specVersion: "v1"
serviceId: "snowpin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "snowpin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
